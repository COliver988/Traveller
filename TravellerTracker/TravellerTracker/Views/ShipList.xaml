<Page
    x:Class="TravellerTracker.Views.ShipList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TravellerTracker.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <StackPanel Orientation="Vertical">
            <ListView Name="lvShips" Margin="3">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition> </ColumnDefinition>
                                <ColumnDefinition> </ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition></RowDefinition>
                                <RowDefinition></RowDefinition>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Column="0" Text="Ship Name" Margin="3"></TextBlock>
                            <StackPanel Orientation="Horizontal">
                                <Button Margin="3" Tag="{Binding ShipId}" Click="btnLoadShip">Edit</Button>
                                <TextBlock Grid.Column="1" Text="{Binding Name}" Margin="3"></TextBlock>
                            </StackPanel>
                            <TextBlock Grid.Column="0" Grid.Row="1" Text="Ship Class" Margin="3"></TextBlock>
                            <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding Class}" Margin="3"></TextBlock>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Page>
