<Page
    x:Class="TravellerTracker.Views.StarportList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TravellerTracker.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/TravDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"></ColumnDefinition>
            <ColumnDefinition Width="200"></ColumnDefinition>
            <ColumnDefinition Width="200"></ColumnDefinition>
            <ColumnDefinition Width="200"></ColumnDefinition>
            <ColumnDefinition Width="100"></ColumnDefinition>
            <ColumnDefinition Width="70"></ColumnDefinition>
            <ColumnDefinition Width="70"></ColumnDefinition>
            <ColumnDefinition Width="70"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="45"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Grid.Column="0" Style="{StaticResource txtBlockCenterBold}" Text="Class"></TextBlock>
        <TextBlock Grid.Column="1" Style="{StaticResource txtBlockCenterBold}" Text="Quality"></TextBlock>
        <TextBlock Grid.Column="2" Style="{StaticResource txtBlockCenterBold}" Text="Repairs"></TextBlock>
        <TextBlock Grid.Column="3" Style="{StaticResource txtBlockCenterBold}" Text="Yards"></TextBlock>
        <TextBlock Grid.Column="4" Style="{StaticResource txtBlockCenterBold}" Width="80" Text="Downport"></TextBlock>
        <TextBlock Grid.Column="5" Style="{StaticResource txtBlockCenterBold}" Text="Refined"></TextBlock>
        <TextBlock Grid.Column="6" Style="{StaticResource txtBlockCenterBold}" Text="Unrefined"></TextBlock>
        <TextBlock Grid.Column="7" Style="{StaticResource txtBlockCenterBold}" Text="Starport"></TextBlock>
        <ListBox Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="8" Name="lstPorts" ItemsSource="{Binding}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                            <ColumnDefinition Width="200"></ColumnDefinition>
                            <ColumnDefinition Width="200"></ColumnDefinition>
                            <ColumnDefinition Width="200"></ColumnDefinition>
                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                            <ColumnDefinition Width="70"></ColumnDefinition>
                            <ColumnDefinition Width="70"></ColumnDefinition>
                            <ColumnDefinition Width="70"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Style="{StaticResource txtBoxNum}" Margin="0" Width="20" Text="{Binding Class, Mode=TwoWay}"></TextBox>
                        <TextBox Grid.Column="1" Style="{StaticResource txtBoxNum}" Width="195" Text="{Binding Quality, Mode=TwoWay}"></TextBox>
                        <TextBox Grid.Column="2" Style="{StaticResource txtBoxNum}" Width="195" Text="{Binding Repairs, Mode=TwoWay}"></TextBox>
                        <TextBox Grid.Column="3" Style="{StaticResource txtBoxNum}" Width="195" Text="{Binding Yards, Mode=TwoWay}"></TextBox>
                        <TextBox Grid.Column="4" Style="{StaticResource txtBoxNum}" Width="95" Text="{Binding Downport, Mode=TwoWay}"></TextBox>
                        <CheckBox Grid.Column="5" IsChecked="{Binding hasRefinedFuel, Mode=TwoWay}" Margin="3"></CheckBox>
                        <CheckBox Grid.Column="6" IsChecked="{Binding hasUnrefinedFuel, Mode=TwoWay}" Margin="3"></CheckBox>
                        <CheckBox Grid.Column="7" IsChecked="{Binding isStarport, Mode=TwoWay}" Margin="3"></CheckBox>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
