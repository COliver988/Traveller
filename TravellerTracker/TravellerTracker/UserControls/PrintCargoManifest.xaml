<UserControl
    x:Class="TravellerTracker.UserControls.PrintCargoManifest"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:TravellerTracker.UserControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/TravDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <RichTextBlock>
        <Paragraph TextAlignment="Left">
            <Run Text="Cargo Manifest for " FontSize="28"></Run>
            <Run Text="{Binding Name}"></Run>
            <Run Text=" on "></Run>
            <Run Text="{Binding Day}"></Run>
            <Run Text=" - "></Run>
            <Run Text="{Binding Day}"></Run>
        </Paragraph>
    </RichTextBlock>
</UserControl>
