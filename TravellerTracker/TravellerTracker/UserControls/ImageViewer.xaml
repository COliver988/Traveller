<UserControl
    x:Class="TravellerTracker.UserControls.ImageViewer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:TravellerTracker.UserControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:my="using:Microsoft.Toolkit.Uwp.UI.Controls"
    d:DesignHeight="600"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid BorderBrush="AliceBlue" Margin="3" BorderThickness="2">
        <my:Carousel
            x:Name="ctlCarousel"
            Margin="3"
            ItemsSource="{Binding}"
            BorderThickness="2"
            ItemDepth="400"
            ItemMargin="50"
            InvertPositive="True"
            ItemRotationY="20"
            SelectedIndex="0"
            BorderBrush="Black">
            <my:Carousel.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition Height="50" />
                        </Grid.RowDefinitions>
                        <Image
                            VerticalAlignment="Bottom"
                            Source="{Binding bmp}"
                            Stretch="Uniform" />
                        <TextBlock Grid.Row="1" Text="{Binding Description}" Margin="3" />
                    </Grid>
                </DataTemplate>
            </my:Carousel.ItemTemplate>
        </my:Carousel>

    </Grid>
</UserControl>
